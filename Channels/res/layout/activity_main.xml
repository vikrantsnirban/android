<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="net.versatile.anroid.channels.MainActivity" >

    <Button
        android:id="@+id/btnAddChannels"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="29dp"
        android:text="@string/addChannelMessage" 
        android:onClick="handleAddChannel"/>

    <Button
        android:id="@+id/btnAddTvChannels"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnAddChannels"
        android:layout_marginTop="47dp"
        android:text="@string/addTvChannelsMessage" 
        android:onClick="handleAddTvChannels"/>

    <Button
        android:id="@+id/btnViewTvChannels"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:text="@string/viewTvChannelMessage" 
        android:onClick="handleViewTvChannels"/>

</RelativeLayout>
